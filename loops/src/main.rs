fn main() {
    // Loops in rust
    while_loop();
}

fn while_loop() {
    // I think, while is the simplest one 
    let mut x = 0;
    while x < 10 {
        println!("{}", x);
        x += 1;
    }
}
